# {{ runbook.title }}

System: {{ runbook.system }}

## Prerequisites
{% if runbook.prerequisites %}
{%- for item in runbook.prerequisites %}
- {{ item }}
{%- endfor %}
{%- else %}
None recorded.
{%- endif %}

## Steps

| # | time | action | target | value | screen | intent | status | note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
{%- for step in runbook.steps %}
{%- set record = status.record_for(step.order) %}
| {{ step.order }} | {{ step.timestamp_s | mmss }} | {{ step.action }} | {{ step.target | cell }} | {{ (step.value or "") | cell }} | {{ step.screen | cell }} | {{ step.intent | cell }} | {{ status.badge_for(step.order) }} | {{ (record.error if record and record.error else (record.check.note if record and record.check else "")) or "" | cell }} |
{%- endfor %}

## Pitfalls
{% if runbook.pitfalls %}
{%- for item in runbook.pitfalls %}
- {{ item }}
{%- endfor %}
{%- else %}
None recorded.
{%- endif %}
